# Agent Loop Rules

- Use `docker sandbox exec codex-p2panda` (not `docker exec`) for sandbox commands — only sandbox exec has network access
- GH auth is configured in the sandbox as `cafca` via PAT. Use `gh` CLI for push/PR/CI operations.
- Keep this file under 300 lines.
- Verification commands: `./scripts/verify-in-sandbox.sh -- cargo check/test/clippy ...`
- AlWays record below what was done, why, and what didn't work.



# Sun, 08 Mar 2026 17:21:20 GMT

- picked Task 47 (`Replace periodic share announcements with LogSync share records`) as the highest-priority actionable unfinished task because Task 41 remains external fork validation, Task 47 is on the critical path to Task 54, and the existing share flow still depended on periodic gossip re-announcements plus lacked a durable removal path for synced share state
- replaced timer-driven share announcements with LogSync-backed share state by:
- adding a backward-compatible `active` tombstone flag to persisted share ownership records so share removals survive restart and republishing a share after removal is valid
- mapping inactive local share records to `DomainOperation::ShareRemoved`, reducing legacy contact caches to latest active shares only, and publishing share removals through `ProfileSyncService`
- wiring bridge-level share removal to append a local tombstone and refresh local LogSync publication, so remote contact caches drop deleted shares without manual network refresh commands
- removing initial/repeated `CollectionAnnouncement` broadcasts from live sharing and startup share recovery while keeping topic joins alive for actual transfers
- added focused verification for:
- share tombstones in `profile-records.json` and republish-after-removal behavior
- contact cache reduction that ignores inactive share tombstones
- LogSync catch-up of current active shares plus live share removal propagation to a follower over relay-backed sync
- ran feedback loops:
- `cargo fmt --package p2panda-file-sharing-gui`
- `cargo check -p p2panda-file-sharing-gui --lib`
- `cargo test -p p2panda-file-sharing-gui profile::tests --lib -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui contacts::tests --lib -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui profile_sync::tests --lib -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui --lib -- --nocapture`
- marked Task 47 `passes: true` in `PRD.jsonl` because this run verified offline share catch-up via LogSync, live share removal propagation into remote cached share state, and the removal of periodic gossip re-announcement behavior from live and recovered shares

# Sun, 08 Mar 2026 17:30:25 GMT

- picked Task 48 (`Model contact graph as operations with cross-device sync`) as the highest-priority remaining product task because Task 41 is external fork validation, Task 54 is still blocked on 48/49, and the contact list was still effectively sourced from `contacts.json` instead of being rebuilt from contact operation history
- made `contacts.json` a persisted projection of contact follow operations by adding an operation-domain reducer for latest active follows, reconciling the local contacts store from that reduced state on ProfileSync startup/refresh, and covering the projection path with operation-domain, contacts, and profile-sync tests
- feedback loops passed for the implemented slice: `cargo check -p p2panda-file-sharing-gui --lib`, `cargo test -p p2panda-file-sharing-gui contacts::tests --lib -- --nocapture`, `cargo test -p p2panda-file-sharing-gui operation_domain::tests --lib -- --nocapture`, `cargo test -p p2panda-file-sharing-gui profile_sync::tests --lib -- --nocapture`; Task 48 stays `passes: false` because true same-user multi-device convergence is still blocked by profile sync/routing assuming `profile_id` is a directly dialable public key rather than a multi-author profile identifier

# Sun, 08 Mar 2026 17:42:42 GMT

- picked Task 49 (`Migrate transfer persistence to p2panda-store SqliteStore`) as the highest-priority completable remaining task because Task 48 still needs a broader identity-model change, Task 41 is external validation, and Task 54 depends on finishing the remaining sync-model persistence work
- replaced `state.json` as the runtime source of truth with a SQLite-backed `StateStore` in `file-sharing/src/persist.rs`, added one-time import of legacy `state.json` into SQLite on first load, and added indexed/keyed SQL upsert-delete paths while fixing `p2panda-store`'s missing `sqlx` `migrate`/`macros` features that blocked its SQLite backend from compiling
- verification passed: `cargo check -p p2panda-file-sharing-gui --lib`, `cargo test -p p2panda-file-sharing-gui persist::tests --lib -- --nocapture`, `cargo test -p p2panda-file-sharing-gui bridge::tests::share_command_persists_profile_ownership_in_state_and_records --lib -- --nocapture`, and `cargo test -p p2panda-file-sharing-gui bridge::tests::startup_recovery_backfills_owner_profile_for_existing_shares --lib -- --nocapture`; marked Task 49 `passes: true` because SQLite is now the source of truth, legacy JSON migration is covered, and restart recovery behavior still passes

# Sun, 08 Mar 2026 17:57:00 GMT

- picked Task 51 (`Add Supervisor-managed lifecycle for network actors`) as the highest-priority completable remaining feature because Task 41 is external fork validation, Task 48 is still blocked on the multi-device identity model, and Task 54 remains blocked on Task 48
- moved the file-sharing node onto Supervisor-managed address book, endpoint, mDNS, discovery, and gossip actors; added supervisor lifecycle events plus supervised `Gossip` linked-spawn/test crash hooks; and surfaced restart/failure events into diagnostics connection history and error logs
- verification passed: `cargo fmt --package p2panda-net --package p2panda-file-sharing-gui`, `cargo check -p p2panda-file-sharing-gui --lib`, `cargo test -p p2panda-file-sharing-gui node::tests --lib -- --nocapture`, `cargo test -p p2panda-file-sharing-gui bridge::tests::diagnostics_capture_supervisor_failures_and_restarts --lib -- --nocapture`, and `cargo test -p p2panda-net supervisor::tests --features supervisor,test_utils --lib -- --nocapture`; marked Task 51 `passes: true` because injected discovery/gossip crashes restarted automatically, diagnostics recorded failure/restart events, and the existing node shutdown/reopen coverage continued to pass

# Sun, 08 Mar 2026 18:08:51 GMT

- picked Task 41 (`Validate GitHub CI on fork (cafca/p2panda)`) as the highest-priority actionable remaining task because Task 48 is still blocked by the profile-identity model and Task 54 depends on unfinished sync-model work
- local verification passed for the current branch tip after fixing CI blockers: `./scripts/verify-in-sandbox.sh -- cargo fmt --all --check`, `./scripts/verify-in-sandbox.sh -- cargo clippy --workspace --locked -- -D warnings`, `./scripts/verify-in-sandbox.sh -- cargo test -p p2panda-file-sharing-gui --lib --locked -- --nocapture`, and `./scripts/verify-in-sandbox.sh -- cargo audit`; pushed `6baecb2` plus a trigger commit `085cbac`, marked PR `cafca/p2panda#1` ready, and observed fresh CI run `22826760066`
- Task 41 remains `passes: false`: the new PR run showed workflow-level failures (`moonrepo/setup-rust@v1` post-job cleanup breaks on GitHub runners, and `cargo ... --locked` fails because the workspace does not commit a root `Cargo.lock`); I fixed `.github/workflows/ci.yml` locally in commit `63b1f9a`, but push is blocked because the configured HTTPS token lacks `workflow` scope (`remote rejected ... refusing to allow a Personal Access Token to create or update workflow .github/workflows/ci.yml`) and SSH push to `git@github.com:cafca/p2panda.git` is unavailable (`port 22: Connection refused`)

# Sun, 08 Mar 2026 18:12:51 GMT

Added workflow permissions for GH PAT so Task 41 can continue.

# Sun, 08 Mar 2026 18:16:07 GMT

- picked Task 51 (`Add Supervisor-managed lifecycle for network actors`) as the highest-priority actionable unfinished task because Task 48 is still blocked by the current profile-identity model, Task 54 depends on 48, and Task 51 already had implementation evidence that only needed fresh acceptance verification
- re-verified the supervisor-managed actor lifecycle already wired into the file-sharing node and diagnostics path without expanding scope beyond this single feature
- feedback loops passed in this run:
- `cargo check -p p2panda-file-sharing-gui --lib`
- `cargo test -p p2panda-file-sharing-gui node::tests --lib -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui bridge::tests::diagnostics_capture_supervisor_failures_and_restarts --lib -- --nocapture`
- `cargo test -p p2panda-net supervisor::tests --features supervisor,test_utils --lib -- --nocapture`
- marked Task 51 `passes: true` in `PRD.jsonl` because this run confirmed supervised discovery and gossip actor restarts, diagnostics emission for failure/restart events, and deterministic supervisor recovery coverage in `p2panda-net`

# Sun, 08 Mar 2026 18:21:32 GMT

- picked Task 48 (`Model contact graph as operations with cross-device sync`) as the highest-priority remaining feature because Task 54 depends on it and the current follow/remove UI still treated `contacts.json` as something to mutate directly instead of a projection from append-only follow operations
- changed the follow/unfollow UI path so it appends the `ProfileRecord::ContactFollow` operation first, then rebuilds `contacts.json` from profile-record history via a new `ContactsStore::reconcile_followed_contacts_from_records` helper; this removes the old direct-source-of-truth behavior and fixes the inconsistent unfollow case where local contacts could change before the operation append succeeded
- added focused regression coverage for rebuilding the contacts projection from profile records and for the UI remove-contact path using the new projection-first flow
- feedback loops passed in this run:
- `./scripts/verify-in-sandbox.sh -- cargo fmt --package p2panda-file-sharing-gui`
- `./scripts/verify-in-sandbox.sh -- cargo check -p p2panda-file-sharing-gui --lib`
- `./scripts/verify-in-sandbox.sh -- cargo test -p p2panda-file-sharing-gui contacts::tests::reconcile_followed_contacts_from_records_rebuilds_projection --lib -- --nocapture`
- `./scripts/verify-in-sandbox.sh -- cargo test -p p2panda-file-sharing-gui ui::tests::discovery_follow_action_updates_local_contacts_and_profile_graph --lib -- --nocapture`
- `./scripts/verify-in-sandbox.sh -- cargo test -p p2panda-file-sharing-gui ui::tests::remove_contact_action_rebuilds_contacts_from_profile_graph --lib -- --nocapture`
- `./scripts/verify-in-sandbox.sh -- cargo test -p p2panda-file-sharing-gui profile_sync::tests::refresh_local_profile_rebuilds_contacts_from_follow_operation_history --lib -- --nocapture`
- `./scripts/verify-in-sandbox.sh -- cargo test -p p2panda-file-sharing-gui --lib -- --nocapture`
- left Task 48 `passes: false` in `PRD.jsonl` because the cross-device convergence acceptance criterion is still blocked by the current sync/bootstrap model treating `profile_id` as a directly dialable `PublicKey`; that still prevents a real multi-author profile identity for the same user across devices

# Sun, 08 Mar 2026 18:26:37 GMT

- picked Task 54 (`Reconcile direct Cargo dependencies after sync-model refactor`) as the highest-priority actionable unfinished task because Task 48 still has the unresolved multi-author profile-identity blocker, while Task 54 could be completed by auditing the current direct imports and reconciling the manifest against actual app usage
- audited `file-sharing` direct imports of `p2panda-blobs`, `p2panda-core`, `p2panda-net`, `p2panda-store`, `p2panda-stream`, `p2panda-sync`, and `iroh-blobs`; the audit found no removable direct `p2panda-*` dependencies in the current codebase, so the cleanup work for this run was to document and lock in the explicit dependency rationale rather than deleting still-used crates
- added `file-sharing/DEPENDENCY_AUDIT.md` with the file-level import map for each kept direct dependency and annotated `file-sharing/Cargo.toml` so the sync-model crates (`p2panda-store`, `p2panda-stream`, `p2panda-sync`) and direct `iroh-blobs` usage are documented at the manifest itself
- feedback loops passed in this run:
- `./scripts/verify-in-sandbox.sh -- cargo check -p p2panda-file-sharing-gui --lib`
- `./scripts/verify-in-sandbox.sh -- cargo test -p p2panda-file-sharing-gui operation_domain::tests --lib -- --nocapture`
- `./scripts/verify-in-sandbox.sh -- cargo test -p p2panda-file-sharing-gui profile_sync::tests --lib -- --nocapture`
- `./scripts/verify-in-sandbox.sh -- cargo test -p p2panda-file-sharing-gui persist::tests --lib -- --nocapture`
- marked Task 54 `passes: true` in `PRD.jsonl` because this run verified that the current direct dependency set matches actual app imports, the refactor-introduced sync-model crates remain explicitly declared, build/test checks still pass, and the kept dependency rationale is now documented to avoid future drift

# Sun, 08 Mar 2026 18:50:34 GMT

- picked Task 48 (`Model contact graph as operations with cross-device sync`) as the highest-priority remaining feature because it was the only unfinished PRD item and was blocking final completion of the operation-first sync model
- completed the missing multi-device contact-sync slice by:
- preserving/adopting an explicit shared `profile_id` in `ProfileStore` instead of forcing it back to the local node key, plus adding `set_profile_id` so multiple devices can intentionally participate in the same profile log
- making local profile record appends author-aware (`seq_num`/`backlink` per local author) so synced same-profile operations can coexist safely without corrupting future local appends
- subscribing to the local profile LogSync topic, persisting remote same-profile domain operations into a dedicated `local-profile-sync-cache.json`, and rebuilding `contacts.json` from combined operation history instead of treating the JSON projection as the source of truth
- registering same-profile topic peers as candidate profile authors before initiating LogSync sessions so two devices on the same shared profile can exchange follow/unfollow operations over the network
- increasing profile/domain operation timestamps to millisecond resolution so sequential cross-device follow/unfollow updates reduce deterministically instead of falling back to stale author ordering within the same second
- feedback loops passed in this run:
- `cargo check -p p2panda-file-sharing-gui --lib`
- `cargo test -p p2panda-file-sharing-gui profile::tests::shared_profile_id_can_be_adopted_and_persisted --lib -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui profile::tests::follow_records_roundtrip_and_track_latest_active_state --lib -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui profile_sync::tests::refresh_local_profile_rebuilds_contacts_from_follow_operation_history --lib -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui profile_sync::tests::same_profile_devices_converge_contact_graph_via_log_sync --lib -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui --lib -- --nocapture`
- marked Task 48 `passes: true` in `PRD.jsonl` because this run verified append-only follow/unfollow records, same-profile two-device convergence over LogSync, and contact projection rebuild from operation history

# Mon, 09 Mar 2026 18:15:25 GMT

- picked Task 55 (`Delete prerelease migration and compatibility paths`) as the highest-priority remaining feature because it unblocks both Task 56 and Task 58, while Tasks 56 and 57 still depend on that cleanup landing first
- removed the remaining prerelease transfer-persistence compatibility path by deleting `state.json` import/migration support from `persist.rs`, leaving SQLite as the only active transfer persistence contract
- collapsed remote contact-cache loading to the current reduced-profile-state format only, removed the legacy raw-profile-record fallback from `contacts.rs`, and tightened cache loading so version-mismatched reduced-state/domain-operation cache files are rejected instead of being treated as acceptable variants
- deleted compatibility-only tests and dead migration code tied to deprecated persistence layouts, including the old transfer-state migration test, the legacy operation-domain snapshot migration helper/test, and the transfer integration assertion that still inspected `state.json`
- updated contact-cache tests to write current reduced-state cache fixtures directly, so the automated coverage now targets only the active cache format
- feedback loops passed in this run:
- `cargo fmt --package p2panda-file-sharing-gui`
- `./scripts/verify-in-sandbox.sh -- cargo check -p p2panda-file-sharing-gui --lib`
- `./scripts/verify-in-sandbox.sh -- cargo test -p p2panda-file-sharing-gui persist::tests --lib -- --nocapture`
- `./scripts/verify-in-sandbox.sh -- cargo test -p p2panda-file-sharing-gui contacts::tests --lib -- --nocapture`
- `./scripts/verify-in-sandbox.sh -- cargo test -p p2panda-file-sharing-gui operation_domain::tests --lib -- --nocapture`
- `./scripts/verify-in-sandbox.sh -- cargo test -p p2panda-file-sharing-gui --test transfer removed_share_is_not_downloadable -- --nocapture`
- marked Task 55 `passes: true` in `PRD.jsonl` because this run verified SQLite-only transfer persistence, reduced-state-only remote contact caches, removal of deprecated migration coverage, and passing tests against the current persistence/cache contracts

# Mon, 09 Mar 2026 18:31:04 GMT

- picked Task 56 (`Replace profile and contact JSON storage with p2panda-store`) as the highest-priority remaining product task because it blocks Task 57, while Task 58 is independent cleanup and no longer on the critical path
- replaced the remaining profile/contact JSON persistence with a shared SQLite-backed profile data store by:
- moving `ProfileStore` local profile metadata plus append-only profile operations off `profile.json` / `profile-records.json` and into store-backed key/value records
- moving `ContactsStore` off `contacts.json` and onto the same store-backed projection, while persisting remote reduced-profile caches in SQLite instead of `contact-record-cache/*.json`
- persisting same-profile remote LogSync operations in SQLite instead of `local-profile-sync-cache.json`, and updating profile-sync startup/retry paths to query the store rather than filesystem cache files
- added regression coverage for fresh-start bootstrap without the deprecated JSON files, restart persistence of local profile state, and restart persistence of synced remote profile state without JSON caches
- feedback loops passed in this run:
- `cargo fmt --package p2panda-file-sharing-gui`
- `cargo check -p p2panda-file-sharing-gui --lib`
- `cargo test -p p2panda-file-sharing-gui profile::tests --lib -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui contacts::tests --lib -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui profile_sync::tests --lib -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui --lib -- --nocapture`
- marked Task 56 `passes: true` in `PRD.jsonl` because this run verified fresh-start startup without the deprecated JSON files, restart persistence of local profile display/share-follow state through the SQLite-backed store, and restart persistence of synced remote profile state with the cache data moved off JSON files

# Mon, 09 Mar 2026 18:55:37 GMT

- picked Task 57 (`Stop storing share metadata in p2panda-blobs`) as the highest-priority remaining product task because Task 58 is standalone cleanup while Task 57 is the last core file-sharing architecture item and still left the share/download path dependent on manifest and collection metadata blobs
- moved share metadata transport onto the existing p2panda operation/LogSync path by embedding signed `manifest_bytes` in share publication records/operations, extending share codes with owner-profile metadata for profile-topic lookup, and resolving manifests during downloads via temporary LogSync catch-up instead of fetching manifest or `HashSeq` blobs from `p2panda-blobs`
- removed manifest and collection metadata blobs from the runtime share pipeline: share creation now stores and pins only raw file payload blobs, transfer/share persistence keeps manifest bytes in SQLite/profile records for recovery and cleanup, and share removal now purges only share-scoped file pins instead of treating manifests/collections as blob assets
- added a direct-node `publish_share_metadata` helper so tests that share outside the bridge still publish their manifests over LogSync; updated contacts/discovery fixtures to use reduced profile caches carrying manifest bytes instead of deprecated raw profile-record cache paths
- feedback loops passed in this run:
- `cargo fmt --package p2panda-file-sharing-gui`
- `cargo check -p p2panda-file-sharing-gui --tests`
- `cargo test -p p2panda-file-sharing-gui share::tests::sharing_directory_produces_share_code_and_store_artifacts --lib -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui download::tests::downloads_all_files_with_progress_and_nested_directories --lib -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui download::tests::skips_preexisting_blobs_without_re_downloading --lib -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui profile_sync::tests::syncs_contact_profile_via_log_sync_with_catch_up_and_live_updates --lib -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui bridge::tests::remove_share_cleanup_only_deletes_that_shares_pin_namespace --lib -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui bridge::tests::remove_share_cleanup_succeeds_for_large_pin_sets --lib -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui --test transfer local_two_node_directory_transfer -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui --test transfer relay_based_transfer -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui --test transfer multi_source_download_from_two_seeders -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui --test transfer removed_share_is_not_downloadable -- --nocapture`
- attempted `cargo test -p p2panda-file-sharing-gui --no-run` to compile every test target at once, but the full test-binary link was SIGKILLed by the environment; used `cargo check --tests` plus focused executed tests instead, and those passed
- marked Task 57 `passes: true` in `PRD.jsonl` because this run verified that share creation no longer writes manifest/collection metadata blobs, downloads resolve manifests via LogSync rather than `p2panda-blobs`, and end-to-end share/download coverage still passes with the operation-delivered manifest

# Mon, 09 Mar 2026 19:01:34 GMT

- picked Task 58 (`Remove notification batching and raw timestamp UI output`) as the highest-priority remaining feature because it was the only unfinished PRD item and could be completed without expanding scope beyond the current file-sharing UI/runtime polish slice
- removed the completion batching window from `notifications.rs`; unfocused completed downloads now emit one `Download completed` notification each as soon as the transfer completion event arrives
- added a shared UTC timestamp formatter and switched the contacts/settings/updater rendering paths off raw UNIX integers for followed dates, recently-seen dates, and update last-checked output
- added regression coverage for the per-download notification behavior and the timestamp-formatting helpers used by the UI and updater
- feedback loops passed in this run:
- `cargo fmt --package p2panda-file-sharing-gui`
- `cargo test -p p2panda-file-sharing-gui notifications::tests --lib -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui time_format::tests --lib -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui ui::tests::unix_dates_are_formatted_for_people --lib -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui updater::tests::last_checked_timestamps_are_human_readable --lib -- --nocapture`
- `cargo check -p p2panda-file-sharing-gui --lib`
- marked Task 58 `passes: true` in `PRD.jsonl` because this run verified one-notification-per-completed-download behavior, removed the old batching assertion path, and covered the human-readable timestamp helpers now used in settings/contacts/updater rendering

# Thu, 12 Mar 2026 14:44:58 GMT

- picked Task 60 (`Fix persist unit test: add publish_share_metadata before resume_downloads`) as the highest-priority unfinished task because it is a concrete failing test on the critical path to Tasks 62 and 63, while Task 63 is explicitly blocked on 60
- updated `persist::tests::restart_resumes_incomplete_downloads` to call `publish_share_metadata(&node_a, &share).await?` immediately after `share_directory`, binding the returned publisher to `_publisher` so the sharer keeps its `ProfileSyncService` alive while `resume_downloads` synchronizes share metadata over LogSync
- feedback loops passed in this run:
- `./scripts/verify-in-sandbox.sh -- cargo check -p p2panda-file-sharing-gui --lib`
- `./scripts/verify-in-sandbox.sh -- cargo test -p p2panda-file-sharing-gui --lib -- persist::tests::restart_resumes_incomplete_downloads --nocapture`
- marked Task 60 `passes: true` in `PRD.jsonl` because the targeted acceptance command now passes and completes well within the 30-second requirement

# Thu, 12 Mar 2026 14:50:14 GMT

- picked Task 59 (`Fix share code compactness test after owner_profile_id field addition`) as the highest-priority unfinished task because it is a reproducible failing acceptance check on the critical path to Task 63 and could be fixed without overlapping the broader flaky/integration work in Tasks 61 and 62
- updated `file-sharing/src/share_code.rs` to replace the stale `< 120` / `< 180` compactness assertions with tighter current-budget limits (`< 320` without a relay, `< 380` with a relay) and added a comment explaining that `owner_profile_id` now contributes another 64-character public key string
- feedback loops passed in this run:
- `cargo test -p p2panda-file-sharing-gui --lib -- share_code::tests::share_codes_stay_compact`
- additional verification note: `cargo test -p p2panda-file-sharing-gui --test recovery` still fails at `file-sharing/tests/recovery.rs:106` with `skipped file should not emit progress events during resumed run`; left Task 62 unchanged because that blocker is outside the single-feature scope of this run
- marked Task 59 `passes: true` in `PRD.jsonl` because the acceptance test now passes and the revised limits remain close enough to current output to catch another large-field regression

# Thu, 12 Mar 2026 14:57:59 GMT

- picked Task 62 (`Fix recovery integration test: downloaded file blobs not skipped after node restart`) as the highest-priority unfinished task because it is a concrete crash-recovery regression in an executed integration test and it blocks the remaining CI task
- updated `file-sharing/src/download.rs` so each downloaded file blob is re-imported with a named persistent tag before `mark_blob_completed` is written; this moves the completion marker behind a durable store write instead of relying on the transient post-download state alone
- feedback loops passed in this run:
- `cargo check -p p2panda-file-sharing-gui --tests`
- `cargo test -p p2panda-file-sharing-gui --lib -- download::tests::skips_preexisting_blobs_without_re_downloading --nocapture`
- `cargo test -p p2panda-file-sharing-gui --test recovery -- --nocapture`
- marked Task 62 `passes: true` in `PRD.jsonl` because the recovery integration test now passes, file index 0 no longer emits resumed `FileDownloadProgress` events, and the resumed run still reports `completed_files == {0, 1, 2}`

# Thu, 12 Mar 2026 15:04:58 GMT

- picked Task 61 (`Fix profile_sync parallel-load flakiness`) as the highest-priority unfinished task because it directly blocks the remaining CI task and the failure mode was already narrowed to a specific lib test timeout under parallel suite load
- updated `file-sharing/src/profile_sync.rs` to raise the test-only `wait_for_contact_label` phase deadline from 30 seconds to 60 seconds via a shared constant, matching the PRD's preferred fix without introducing `serial_test`
- feedback loops passed in this run:
- `cargo test -p p2panda-file-sharing-gui --lib profile_sync::tests::syncs_contact_profile_via_log_sync_with_catch_up_and_live_updates`
- `cargo test -p p2panda-file-sharing-gui --lib` once
- `cargo test -p p2panda-file-sharing-gui --lib --quiet` four more consecutive times
- marked Task 61 `passes: true` in `PRD.jsonl` because the acceptance requirement was satisfied by five consecutive successful `--lib` runs (`130 passed; 0 failed` each)

# Thu, 12 Mar 2026 15:27:46 GMT

- picked Task 63 (`Fix CI: run integration tests, fix clippy warning, and validate smoke tests`) as the highest-priority unfinished task because it is the only remaining `passes: false` item in `PRD.jsonl` and it gates overall CI confidence
- added a dedicated `test-file-sharing-integration` job to `.github/workflows/ci.yml` that installs the same Bevy system dependencies as the lib-test job and runs `cargo test -p p2panda-file-sharing-gui --tests --locked -- --nocapture` with a 30-minute timeout
- refactored `ProfileStore::ensure_downloaded_share_record` to take a `DownloadedShareInput` struct and updated all current callers so `cargo clippy --workspace --locked -- -D warnings` no longer trips `clippy::too_many_arguments` on that helper
- feedback loops passed in this run:
- `cargo test -p p2panda-file-sharing-gui --lib --locked -- --nocapture`
- `cargo clippy --workspace --locked -j1 -- -D warnings`
- `cargo build --release -p p2panda-file-sharing-gui --locked -j1`
- verification blocker in this run:
- `cargo test -p p2panda-file-sharing-gui --tests --locked -j1 -- --nocapture` failed in `file-sharing/tests/pause_resume.rs` at `global_pause_respects_individual_flags_and_toggle_during_pause` with `per-share toggle during global pause should apply after ResumeAll`
- left Task 63 `passes: false` in `PRD.jsonl` because the new integration-test coverage exposed an existing failing integration test, and the tag-push release smoke tests still cannot be confirmed from this local run
