# Agent Loop Rules

- Use `docker sandbox exec codex-p2panda` (not `docker exec`) for sandbox commands — only sandbox exec has network access
- GH auth is configured in the sandbox as `cafca` via PAT. Use `gh` CLI for push/PR/CI operations.
- Keep this file under 300 lines.
- Verification commands: `./scripts/verify-in-sandbox.sh -- cargo check/test/clippy ...`



# Sun, 08 Mar 2026 17:21:20 GMT

- picked Task 47 (`Replace periodic share announcements with LogSync share records`) as the highest-priority actionable unfinished task because Task 41 remains external fork validation, Task 47 is on the critical path to Task 54, and the existing share flow still depended on periodic gossip re-announcements plus lacked a durable removal path for synced share state
- replaced timer-driven share announcements with LogSync-backed share state by:
- adding a backward-compatible `active` tombstone flag to persisted share ownership records so share removals survive restart and republishing a share after removal is valid
- mapping inactive local share records to `DomainOperation::ShareRemoved`, reducing legacy contact caches to latest active shares only, and publishing share removals through `ProfileSyncService`
- wiring bridge-level share removal to append a local tombstone and refresh local LogSync publication, so remote contact caches drop deleted shares without manual network refresh commands
- removing initial/repeated `CollectionAnnouncement` broadcasts from live sharing and startup share recovery while keeping topic joins alive for actual transfers
- added focused verification for:
- share tombstones in `profile-records.json` and republish-after-removal behavior
- contact cache reduction that ignores inactive share tombstones
- LogSync catch-up of current active shares plus live share removal propagation to a follower over relay-backed sync
- ran feedback loops:
- `cargo fmt --package p2panda-file-sharing-gui`
- `cargo check -p p2panda-file-sharing-gui --lib`
- `cargo test -p p2panda-file-sharing-gui profile::tests --lib -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui contacts::tests --lib -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui profile_sync::tests --lib -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui --lib -- --nocapture`
- marked Task 47 `passes: true` in `PRD.jsonl` because this run verified offline share catch-up via LogSync, live share removal propagation into remote cached share state, and the removal of periodic gossip re-announcement behavior from live and recovered shares

# Sun, 08 Mar 2026 17:30:25 GMT

- picked Task 48 (`Model contact graph as operations with cross-device sync`) as the highest-priority remaining product task because Task 41 is external fork validation, Task 54 is still blocked on 48/49, and the contact list was still effectively sourced from `contacts.json` instead of being rebuilt from contact operation history
- made `contacts.json` a persisted projection of contact follow operations by adding an operation-domain reducer for latest active follows, reconciling the local contacts store from that reduced state on ProfileSync startup/refresh, and covering the projection path with operation-domain, contacts, and profile-sync tests
- feedback loops passed for the implemented slice: `cargo check -p p2panda-file-sharing-gui --lib`, `cargo test -p p2panda-file-sharing-gui contacts::tests --lib -- --nocapture`, `cargo test -p p2panda-file-sharing-gui operation_domain::tests --lib -- --nocapture`, `cargo test -p p2panda-file-sharing-gui profile_sync::tests --lib -- --nocapture`; Task 48 stays `passes: false` because true same-user multi-device convergence is still blocked by profile sync/routing assuming `profile_id` is a directly dialable public key rather than a multi-author profile identifier

# Sun, 08 Mar 2026 17:42:42 GMT

- picked Task 49 (`Migrate transfer persistence to p2panda-store SqliteStore`) as the highest-priority completable remaining task because Task 48 still needs a broader identity-model change, Task 41 is external validation, and Task 54 depends on finishing the remaining sync-model persistence work
- replaced `state.json` as the runtime source of truth with a SQLite-backed `StateStore` in `file-sharing/src/persist.rs`, added one-time import of legacy `state.json` into SQLite on first load, and added indexed/keyed SQL upsert-delete paths while fixing `p2panda-store`'s missing `sqlx` `migrate`/`macros` features that blocked its SQLite backend from compiling
- verification passed: `cargo check -p p2panda-file-sharing-gui --lib`, `cargo test -p p2panda-file-sharing-gui persist::tests --lib -- --nocapture`, `cargo test -p p2panda-file-sharing-gui bridge::tests::share_command_persists_profile_ownership_in_state_and_records --lib -- --nocapture`, and `cargo test -p p2panda-file-sharing-gui bridge::tests::startup_recovery_backfills_owner_profile_for_existing_shares --lib -- --nocapture`; marked Task 49 `passes: true` because SQLite is now the source of truth, legacy JSON migration is covered, and restart recovery behavior still passes

# Sun, 08 Mar 2026 17:57:00 GMT

- picked Task 51 (`Add Supervisor-managed lifecycle for network actors`) as the highest-priority completable remaining feature because Task 41 is external fork validation, Task 48 is still blocked on the multi-device identity model, and Task 54 remains blocked on Task 48
- moved the file-sharing node onto Supervisor-managed address book, endpoint, mDNS, discovery, and gossip actors; added supervisor lifecycle events plus supervised `Gossip` linked-spawn/test crash hooks; and surfaced restart/failure events into diagnostics connection history and error logs
- verification passed: `cargo fmt --package p2panda-net --package p2panda-file-sharing-gui`, `cargo check -p p2panda-file-sharing-gui --lib`, `cargo test -p p2panda-file-sharing-gui node::tests --lib -- --nocapture`, `cargo test -p p2panda-file-sharing-gui bridge::tests::diagnostics_capture_supervisor_failures_and_restarts --lib -- --nocapture`, and `cargo test -p p2panda-net supervisor::tests --features supervisor,test_utils --lib -- --nocapture`; marked Task 51 `passes: true` because injected discovery/gossip crashes restarted automatically, diagnostics recorded failure/restart events, and the existing node shutdown/reopen coverage continued to pass

# Sun, 08 Mar 2026 18:08:51 GMT

- picked Task 41 (`Validate GitHub CI on fork (cafca/p2panda)`) as the highest-priority actionable remaining task because Task 48 is still blocked by the profile-identity model and Task 54 depends on unfinished sync-model work
- local verification passed for the current branch tip after fixing CI blockers: `./scripts/verify-in-sandbox.sh -- cargo fmt --all --check`, `./scripts/verify-in-sandbox.sh -- cargo clippy --workspace --locked -- -D warnings`, `./scripts/verify-in-sandbox.sh -- cargo test -p p2panda-file-sharing-gui --lib --locked -- --nocapture`, and `./scripts/verify-in-sandbox.sh -- cargo audit`; pushed `6baecb2` plus a trigger commit `085cbac`, marked PR `cafca/p2panda#1` ready, and observed fresh CI run `22826760066`
- Task 41 remains `passes: false`: the new PR run showed workflow-level failures (`moonrepo/setup-rust@v1` post-job cleanup breaks on GitHub runners, and `cargo ... --locked` fails because the workspace does not commit a root `Cargo.lock`); I fixed `.github/workflows/ci.yml` locally in commit `63b1f9a`, but push is blocked because the configured HTTPS token lacks `workflow` scope (`remote rejected ... refusing to allow a Personal Access Token to create or update workflow .github/workflows/ci.yml`) and SSH push to `git@github.com:cafca/p2panda.git` is unavailable (`port 22: Connection refused`)

# Sun, 08 Mar 2026 18:12:51 GMT

Added workflow permissions for GH PAT so Task 41 can continue.

# Sun, 08 Mar 2026 18:16:07 GMT

- picked Task 51 (`Add Supervisor-managed lifecycle for network actors`) as the highest-priority actionable unfinished task because Task 48 is still blocked by the current profile-identity model, Task 54 depends on 48, and Task 51 already had implementation evidence that only needed fresh acceptance verification
- re-verified the supervisor-managed actor lifecycle already wired into the file-sharing node and diagnostics path without expanding scope beyond this single feature
- feedback loops passed in this run:
- `cargo check -p p2panda-file-sharing-gui --lib`
- `cargo test -p p2panda-file-sharing-gui node::tests --lib -- --nocapture`
- `cargo test -p p2panda-file-sharing-gui bridge::tests::diagnostics_capture_supervisor_failures_and_restarts --lib -- --nocapture`
- `cargo test -p p2panda-net supervisor::tests --features supervisor,test_utils --lib -- --nocapture`
- marked Task 51 `passes: true` in `PRD.jsonl` because this run confirmed supervised discovery and gossip actor restarts, diagnostics emission for failure/restart events, and deterministic supervisor recovery coverage in `p2panda-net`
